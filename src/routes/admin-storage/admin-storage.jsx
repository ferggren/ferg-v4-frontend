'use strict';

import React from 'react';
import './styles';

const propTypes = {

};

const defaultProps = {

};

class AdminStorage extends React.PureComponent {
  render() {
    return (
      <div>
        AdminStorage
      </div>
    );
  }
}

AdminStorage.propTypes = propTypes;
AdminStorage.defaultProps = defaultProps;

export default AdminStorage;
